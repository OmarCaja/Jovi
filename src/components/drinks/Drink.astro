---
import type { DrinkItem } from "../../types/drinks/drinkItem";

import "../../styles/components/drinks/Drink.less";

interface Props {
	drink: DrinkItem;
}

const { drink } = Astro.props;
---

<li class="drink">
	<div
		class="neon-underline flex flex-direction justify-content"
		style="--direction: row; --justify: space-between"
	>
		<h5>
			{drink.title}
		</h5>
		<h5>
			{drink.price} €
		</h5>
	</div>
	<p>{drink.description}</p>
</li>
